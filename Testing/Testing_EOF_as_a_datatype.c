#include <stdio.h>
#include <stdlib.h>
int main()
{
/*
    int x = EOF;
    printf("The value of EOF = %d\n", x);\
*/

/*
    int c;
    while ( c = (getchar() != EOF) ) {
        printf("%d\n", c);
            }
        printf("%d\n", c);

*/
//    printf("%c", EOF); // is not printing
//    printf("%d", EOF);  // this does

        printf("The value of EOF = %d\n", EOF);
        printf("The value of EOF = %d\n", EOF + 1); //we can do math operations with it
}
